Implement the ticket (use implement skill) from the given url.
When the ticket is fully solved (all acceptance criteria met, tests green, issue closed), create the file .opencode-loop/<N>/done. 
Do NOT create it otherwise.
Commit changes if ticket is fully solved.
